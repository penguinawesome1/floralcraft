[package]
name = "lattice"
version.workspace = true
edition.workspace = true
license = "MIT"
description = "Grid-based spatial indexing and utility structures for coordinate-aware data in the Floralcraft engine."

[dependencies]
aether = { path = "../aether" }
fastnoise2 = "0.4"
glam = "0.30"
