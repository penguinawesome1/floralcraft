[package]
name = "blueprint"
version.workspace = true
edition.workspace = true
license = "MIT"
description = "Schema-driven voxel definitions and structural templates for the Floralcraft engine."

[dependencies]
blueprint_macros = { path = "macros" }
